import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.Payroll.SmartObjective';

export default defineMessages({
  objective: {
    id: `${scope}.objective`,
    defaultMessage: 'objective',
  },

  measurementTool: {
    id: `${scope}.measurementTool`,
    defaultMessage: 'measurementTool',
  },

  dueDate: {
    id: `${scope}.dueDate`,
    defaultMessage: 'dueDate',
  },

  achieved: {
    id: `${scope}.achieved`,
    defaultMessage: 'achieved',
  },

  weight: {
    id: `${scope}.weight`,
    defaultMessage: 'weight',
  },

  weightedScore: {
    id: `${scope}.weightedScore`,
    defaultMessage: 'weightedScore',
  },

  comment: {
    id: `${scope}.comment`,
    defaultMessage: 'comment',
  },

  employeeComment: {
    id: `${scope}.employeeComment`,
    defaultMessage: 'employeeComment',
  },

  department: {
    id: `${scope}.department`,
    defaultMessage: 'department',
  },

  averageExecutionOfObjective: {
    id: `${scope}.averageExecutionOfObjective`,
    defaultMessage: 'averageExecutionOfObjective',
  },

  totalWeight: {
    id: `${scope}.totalWeight`,
    defaultMessage: 'totalWeight',
  },

  totalScore: {
    id: `${scope}.totalScore`,
    defaultMessage: 'totalScore',
  },
});
