import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.Payroll.survey';

export default defineMessages({
  noChoicesFound: {
    id: `${scope}.noChoicesFound`,
    defaultMessage: 'noChoicesFound',
  },
  addOrChangeChoice: {
    id: `${scope}.addOrChangeChoice`,
    defaultMessage: 'addOrChangeChoice',
  },
  choicesInfo: {
    id: `${scope}.choicesInfo`,
    defaultMessage: 'choicesInfo',
  },
  editChoice: {
    id: `${scope}.editChoice`,
    defaultMessage: 'editChoice',
  },
  addChoice: {
    id: `${scope}.addChoice`,
    defaultMessage: 'addChoice',
  },
});
